# Inference Layer

This folder will host wrappers for:

- YOLO based license plate detection
- OCR recognizer
- MediaPipe pose and hands inference
- Gesture classification logic

The current implementation keeps these integrations as placeholders so the API and UI can be developed in parallel.
